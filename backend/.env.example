# =============================================================================
# Backend Environment Variables Template
# Copy to .env.production on the Hostinger VPS — NEVER commit this file
# =============================================================================

# ── Server ────────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8080
PORT=8080
RUST_LOG=info
ENVIRONMENT=production

# ── JWT ───────────────────────────────────────────────────────────────────────
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters
JWT_REFRESH_SECRET=your-super-secret-refresh-key-minimum-32-characters
JWT_EXPIRY_HOURS=24
JWT_REFRESH_EXPIRY_DAYS=30

# ── CORS (must exactly match the browser's Origin header) ────────────────────
# Production: frontend on same host, served by nginx
FRONTEND_URL=https://www.mvrconsultants.org
ALLOWED_ORIGINS=https://www.mvrconsultants.org

# ── Cloudinary (image uploads) ────────────────────────────────────────────────
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
CLOUDINARY_UPLOAD_PRESET=mvr_consultants

# ── Email — Resend (https://resend.com) ───────────────────────────────────────
RESEND_API_KEY=re_your_api_key
# Must be a verified sender domain in Resend dashboard
EMAIL_FROM=noreply@mvrconsultants.org
EMAIL_FROM_NAME=MVR Consultants
# Hyderabad office admin notification recipient
ADMIN_EMAIL=mvrconsultantshyd@gmail.com
# Guntur office — business email from Hostinger
ADMIN_EMAIL_GUNTUR=guntur@mvrconsultants.org

# ── AI — Google Gemini (SOP reviewer) ────────────────────────────────────────
# Get key at https://aistudio.google.com/app/apikey
# Free tier: 15 req/min · Paid: ~$0.0003/SOP review (Gemini 1.5 Flash)
GEMINI_API_KEY=AIzaSy_your_gemini_api_key_here
